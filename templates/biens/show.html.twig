{% extends 'base.html.twig' %}

{% block title %}Biens
{% endblock %}

{% block body %}
	<h1>Bien</h1>

	<table class="table">
		<tbody>

			<tr>
				<th>Titre</th>
				<td>{{ bien.title }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ bien.description | u.truncate(350, "...") | raw }}</td>
			</tr>
		</tbody>
	</table>
	<div class="text-center">
		<a href="{{ path('biens_index') }}">Retour à la liste
		</a>
	</div>
	<div class="text-right">
		<a href="{{ path('biens_edit', {'id': bien.id}) }}">Modifer</a>
	</div>
	<div class="text-right">
		{{ include('biens/_delete_form.html.twig') }}
	</div>
{% endblock %}
